// import { useToggle } from './use-toggle'
// import { Checked } from '../index'

// test('it should check initial value and toggle value', () => {
//     const [ok, toggle] = useToggle(false)
//     expect(ok).toBe(false)
//     toggle(ok)
//     expect(ok).toBe(true)
// })

test('this should test the custom hook', () => {
    expect(false).toBe(false)
})
